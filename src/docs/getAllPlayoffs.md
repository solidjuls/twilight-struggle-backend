# Feature: Create an endpoint that return all playoffs names and id's

## Context
We need to create an endpoint that returns all playoffs names and id's so we can display them in the playoffs page

## Goal
Create an endpoint that returns all playoffs names and id's

## Requirements
- The endpoint should be a GET request
- The endpoint should be at /playoffs/getAll
